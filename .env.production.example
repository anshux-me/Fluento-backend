# Production Environment Variables for Fluento Backend (Render)
# Configure these in Render Dashboard > Environment

# Debug mode - keep false for production
DEBUG=false

# Whisper model size: tiny.en (fastest), base.en (balanced), small.en (accurate)
# Use .en suffix for English-only (faster and smaller)
# Note: Larger models require more RAM and have slower cold starts
WHISPER_MODEL=tiny.en

# TTS model (using gTTS, no config needed)
TTS_MODEL=tts_models/en/ljspeech/tacotron2-DDC

# Data directory (use default on Render)
DATA_DIR=/app/data

# Firebase credentials - TWO OPTIONS:
# Option 1: Path to credentials file (use Secret Files in Render)
FIREBASE_CREDENTIALS_PATH=/app/firebase-credentials.json

# Option 2: Inline JSON (paste entire firebase-credentials.json content)
# FIREBASE_CREDENTIALS_JSON={"type":"service_account","project_id":"..."}

# CORS origins - Add your Vercel frontend URL
# Comma-separated list of allowed origins
CORS_ORIGINS=https://your-app.vercel.app,https://your-custom-domain.com
